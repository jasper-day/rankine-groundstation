# MPCC 

This package provides an implementation of model predictive 
contouring control, which is the control scheme powering 
EvenFlow.

The high performance code is written in C++ with Python bindings
provided by `scikit-build-core`.
